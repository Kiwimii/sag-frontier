(()=>{'use strict';const G=window.SAGGalia;if(!G)return;let busy=false;const guarded=(name,fn)=>function(...args){if(busy)return{ok:false,msg:'Aktion läuft bereits.'};busy=true;const snapshot=JSON.stringify(G.state);try{return fn(...args)}catch(error){try{Object.assign(G.state,JSON.parse(snapshot));G.save()}catch{}console.error(`Galia ${name} failed`,error);return{ok:false,msg:'Aktion wurde sicher abgebrochen.'}}finally{setTimeout(()=>{busy=false},120)}};const travel=G.travel.bind(G);G.travel=guarded('travel',id=>{if(id===G.state.location)return{ok:false,msg:'Du bist bereits in diesem Sektor.'};const target=G.C.sectors.find(x=>x.id===id);if(!target)return{ok:false,msg:'Sektor unbekannt.'};const cost=Math.max(1,Number(target.distance||1)-(Number(G.state.ship.engine||1)-1));if(!Number.isFinite(cost)||cost<1)return{ok:false,msg:'Route konnte nicht berechnet werden.'};return travel(id)});const trade=G.trade.bind(G);G.trade=guarded('trade',(id,dir)=>{if(!['buy','sell'].includes(dir))return{ok:false,msg:'Ungültige Handelsrichtung.'};if(!G.C.goods.some(x=>x.id===id))return{ok:false,msg:'Ware unbekannt.'};trade(id,dir);return{ok:true}});const upgrade=G.upgrade.bind(G);G.upgrade=guarded('upgrade',key=>{if(!['scanner','engine','shield','ai','stealth','drones','cargo'].includes(key))return{ok:false,msg:'Modul unbekannt.'};upgrade(key);return{ok:true}});const build=G.build.bind(G);G.build=guarded('build',id=>{if(!G.C.facilities.some(x=>x.id===id))return{ok:false,msg:'Ausbau unbekannt.'};build(id);return{ok:true}});const campaign=G.chooseCampaign.bind(G);G.chooseCampaign=guarded('campaign',index=>{const scene=G.C.campaign[G.state.campaignStep];if(!scene||!scene.choices?.[index])return{ok:false,msg:'Entscheidung nicht verfügbar.'};campaign(index);return{ok:true}});window.SAGGaliaSafety={get busy(){return busy}}})();