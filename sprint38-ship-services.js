(()=>{'use strict';const G=window.SAGGalia;if(!G)return;function refuel(units=1){units=Math.max(1,Math.min(10,Math.floor(Number(units)||1)));const max=12+Math.max(0,(G.state.ship.engine||1)-1)*2,space=Math.max(0,max-G.state.ship.fuel),amount=Math.min(units,space),cost=amount*35;if(amount<1)return{ok:false,msg:'Treibstofftank ist bereits voll.'};if(G.state.credits<cost)return{ok:false,msg:`${cost} C für ${amount} Treibstoff benötigt.`};G.state.credits-=cost;G.state.ship.fuel+=amount;G.log(`${amount} Treibstoff geladen: ${cost} C.`);return{ok:true,msg:`${amount} Treibstoff geladen.`}}function repair(points=10){points=Math.max(1,Math.min(50,Math.floor(Number(points)||10)));const missing=Math.max(0,100-G.state.ship.hull),amount=Math.min(points,missing),cost=amount*4;if(amount<1)return{ok:false,msg:'Schiffshülle ist vollständig intakt.'};if(G.state.credits<cost)return{ok:false,msg:`${cost} C für ${amount} Hüllenpunkte benötigt.`};G.state.credits-=cost;G.state.ship.hull+=amount;G.log(`Schiffshülle um ${amount} Punkte repariert: ${cost} C.`);return{ok:true,msg:`Hülle auf ${G.state.ship.hull}%.`}}function sellCargo(){const sector=G.C.sectors.find(x=>x.id===G.state.location),excluded=new Set(['fuel']),sale=[];let total=0;for(const good of G.C.goods){const qty=Math.max(0,Math.floor(G.state.cargo[good.id]||0));if(!qty||excluded.has(good.id))continue;const price=Math.max(1,Math.round(good.base*(sector?.market?.[good.id]||1)));total+=qty*price;sale.push(`${qty}× ${good.name}`);G.state.cargo[good.id]=0}if(!sale.length)return{ok:false,msg:'Keine verkaufbare Fracht vorhanden.'};G.state.credits+=total;G.log(`Fracht verkauft (${sale.join(', ')}): ${total} C.`);return{ok:true,msg:`Fracht für ${total} C verkauft.`}}window.SAGGaliaServices={refuel,repair,sellCargo,maxFuel(){return 12+Math.max(0,(G.state.ship.engine||1)-1)*2}}})();