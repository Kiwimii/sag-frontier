const fs = require('fs');

const input = 'sprint09-source.js';
const output = 'sprint10-source.js';
let source = fs.readFileSync(input, 'utf8');

function replaceLine(prefix, replacement, label = prefix) {
  const lines = source.split('\n');
  const index = lines.findIndex(line => line.startsWith(prefix));
  if (index < 0) throw new Error(`Missing ${label}`);
  lines[index] = replacement;
  source = lines.join('\n');
}
function insertAfterLine(prefix, addition, label = prefix) {
  const lines = source.split('\n');
  const index = lines.findIndex(line => line.startsWith(prefix));
  if (index < 0) throw new Error(`Missing ${label}`);
  lines.splice(index + 1, 0, addition);
  source = lines.join('\n');
}
function replaceExact(needle, replacement, label = needle.slice(0, 80)) {
  if (!source.includes(needle)) throw new Error(`Missing ${label}`);
  source = source.replace(needle, replacement);
}

replaceLine("const defaults=()=>", "const defaults=()=>({version:10,language:'de',credits:250,cores:0,skillPoints:0,best:0,pilotXp:0,pilotRank:1,selectedSector:'outer',difficulty:'explorer',adaptiveAssist:0,techPity:0,explorationBest:0,sectorProgress:{outer:1,debris:0,crimson:0},weapons:{laser:{unlocked:true,level:1},rail:{unlocked:false,level:0},rocket:{unlocked:false,level:0},plasma:{unlocked:false,level:0},arc:{unlocked:false,level:0},flak:{unlocked:false,level:0}},ships:{vanguard:{unlocked:true,level:1},interceptor:{unlocked:false,level:0},bastion:{unlocked:false,level:0},specter:{unlocked:false,level:0},carrier:{unlocked:false,level:0}},modules:{targeting:{unlocked:true,level:1},salvage:{unlocked:false,level:0},capacitor:{unlocked:false,level:0},armor:{unlocked:false,level:0},magnet:{unlocked:false,level:0},amplifier:{unlocked:false,level:0}},skills:{hull:0,speed:0,recovery:0,lastStand:0,damage:0,crit:0,chipTech:0,overcharge:0,cooldown:0,salvage:0,magnetism:0,coreHunter:0},loadout:{ship:'vanguard',primary:'laser',secondary:null,module:'targeting'},stats:{runs:0,kills:0,bosses:0,creditsEarned:0,skillPointsEarned:0,pilotXpEarned:0,discoveries:0,stations:0,asteroids:0}});", 'defaults');
replaceExact("out.best=Math.max(0,Math.floor(Number(out.best)||0));", "out.best=Math.max(0,Math.floor(Number(out.best)||0));out.explorationBest=Math.max(0,Math.floor(Number(out.explorationBest)||0));", 'exploration save normalization');

replaceExact("I.de.title='S.A.G. FRONTIER // FRONTIER PATH 0.9';I.en.title='S.A.G. FRONTIER // FRONTIER PATH 0.9';", "I.de.title='S.A.G. FRONTIER // DEEP SPACE 0.10';I.en.title='S.A.G. FRONTIER // DEEP SPACE 0.10';", 'version title');
insertAfterLine("Object.assign(I.de,{pilotRank:", "Object.assign(I.de,{distance:'DISTANZ',discoveries:'ENTDECKUNGEN',station:'RAUMSTATION',resourceAsteroid:'RESSOURCEN-ASTEROID',stationFound:'RAUMSTATION ENTDECKT',asteroidFound:'RESSOURCENFELD ENTDECKT',stationSalvaged:'STATIONSDATEN GEBORGEN',asteroidMined:'ASTEROID ABGEBAUT',scan:'BERGUNG',explore:'ERKUNDE',directiveExplore:'ERKUNDE {n} EINHEITEN',deepSpace:'PROZEDURALER TIEFENRAUM',deepSpaceDesc:'Der Raum erweitert sich in Flugrichtung. Entdecke Stationen, Ressourcenfelder und neue taktische Wege.',passive:'SCHIFFSPASSIVE',evolution:'WAFFEN-EVOLUTION'});Object.assign(I.en,{distance:'DISTANCE',discoveries:'DISCOVERIES',station:'SPACE STATION',resourceAsteroid:'RESOURCE ASTEROID',stationFound:'SPACE STATION DISCOVERED',asteroidFound:'RESOURCE FIELD DISCOVERED',stationSalvaged:'STATION DATA SALVAGED',asteroidMined:'ASTEROID MINED',scan:'SALVAGE',explore:'EXPLORE',directiveExplore:'EXPLORE {n} UNITS',deepSpace:'PROCEDURAL DEEP SPACE',deepSpaceDesc:'Space expands as you travel. Discover stations, resource fields, and new tactical routes.',passive:'SHIP PASSIVE',evolution:'WEAPON EVOLUTION'});", 'deep space translations');
insertAfterLine("I.de.skillDesc=", "Object.assign(I.de.upgradeNames,{plasmaField:'Resonanzfeld',arcChain:'Kettenentladung',flakGuard:'Abfangflak'});Object.assign(I.en.upgradeNames,{plasmaField:'Resonance Field',arcChain:'Chain Discharge',flakGuard:'Interceptor Flak'});Object.assign(I.de.upgradeDescs,{plasmaField:'Plasmaeinschläge hinterlassen ein kurzlebiges Schadensfeld.',arcChain:'Lichtbogen springt auf ein zusätzliches Ziel.',flakGuard:'Flakgeschosse zerstören feindliche Projektile.'});Object.assign(I.en.upgradeDescs,{plasmaField:'Plasma impacts leave a short-lived damage field.',arcChain:'Arc discharge chains to one additional target.',flakGuard:'Flak pellets destroy hostile projectiles.'});", 'weapon evolution translations');

replaceLine("let W=", "let W=innerWidth,H=innerHeight,dpr=Math.min(devicePixelRatio||1,2),state='hangar',last=performance.now(),stars=[],cameraX=0,cameraY=0;", 'camera state');
replaceLine("let player,", "let player,enemies=[],bullets=[],enemyBullets=[],rockets=[],plasmaBolts=[],beams=[],particles=[],pickups=[],explosions=[],fields=[],worldObjects=[],boss=null;", 'world arrays');
replaceLine("let score=", "let score=0,elapsed=0,wave=1,sector=1,waveClock=0,spawnTimer=0,level=1,xp=0,xpNeed=34,kills=0,bossesKilled=0,eliteKills=0,combo=1,maxCombo=1,comboClock=0,shake=0,toastTimer=0,pendingMajor=0,lastMajorAt=0,directive=null,waveKills=0,wavePickups=0,weaponChipsDropped=0,salvageCredits=0,salvageCores=0,discoveries=0,runStations=0,runAsteroids=0,explorationMax=0,directiveStartDistance=0;", 'run state');
replaceLine("function resize()", "function resize(){W=innerWidth;H=innerHeight;dpr=Math.min(devicePixelRatio||1,2);canvas.width=Math.floor(W*dpr);canvas.height=Math.floor(H*dpr);canvas.style.width=W+'px';canvas.style.height=H+'px';ctx.setTransform(dpr,0,0,dpr,0,0)}", 'unbounded resize');

const worldCode = `const CHUNK_SIZE=900,WORLD_RADIUS=2,worldChunks=new Map();
function hashSeed(x,y,s=0){let h=(x*374761393+y*668265263+s*1442695041)|0;h=(h^(h>>>13))*1274126177;return(h^(h>>>16))>>>0}
function seeded(x,y,s=0){let h=hashSeed(x,y,s);return()=>{h=(h*1664525+1013904223)>>>0;return h/4294967296}}
function chunkKey(x,y){return x+':'+y}
function generateChunk(cx,cy){const key=chunkKey(cx,cy);if(worldChunks.has(key))return;worldChunks.set(key,true);const rnd=seeded(cx,cy,sectorDefs[save.selectedSector].index),baseX=cx*CHUNK_SIZE,baseY=cy*CHUNK_SIZE,origin=cx===0&&cy===0,count=origin?2:1+Math.floor(rnd()*3);for(let i=0;i<count;i++){const x=baseX+110+rnd()*(CHUNK_SIZE-220),y=baseY+110+rnd()*(CHUNK_SIZE-220),r=48+rnd()*42,hp=42+rnd()*54;worldObjects.push({worldObject:true,type:'asteroid',x,y,r,hp,maxHp:hp,phase:rnd()*8,spin:(rnd()-.5)*.35,dead:false,looted:false,discovered:false,hitCooldown:0,chunk:key})}if(!origin&&rnd()<.14){const x=baseX+180+rnd()*(CHUNK_SIZE-360),y=baseY+180+rnd()*(CHUNK_SIZE-360);worldObjects.push({worldObject:true,type:'station',x,y,r:78,phase:rnd()*8,scan:0,dead:false,looted:false,discovered:false,chunk:key})}}
function ensureWorld(){if(!player)return;const cx=Math.floor(player.x/CHUNK_SIZE),cy=Math.floor(player.y/CHUNK_SIZE);for(let x=cx-WORLD_RADIUS;x<=cx+WORLD_RADIUS;x++)for(let y=cy-WORLD_RADIUS;y<=cy+WORLD_RADIUS;y++)generateChunk(x,y)}
function discoverWorldObject(o){if(o.discovered)return;o.discovered=true;discoveries++;score+=75;showToast(o.type==='station'?t().stationFound:t().asteroidFound);addParticles(o.x,o.y,o.type==='station'?'#55eaff':'#d7a83c',18,150)}
function salvageStation(o){if(o.looted)return;o.looted=true;o.scan=0;runStations++;const value=95+Math.floor(Math.random()*66);salvageCredits+=value;score+=220;player.hp=Math.min(player.maxHp,player.hp+18);player.shield=Math.min(player.maxShield,player.shield+18);for(let i=0;i<3;i++)pickups.push({x:o.x+(Math.random()-.5)*100,y:o.y+(Math.random()-.5)*100,type:i===2&&Math.random()<.35?'tech':'credits',chip:null,value:i===2?1:25+Math.floor(Math.random()*24),r:12,life:20,phase:Math.random()*6,dead:false});showToast(t().stationSalvaged+' // +'+value+' '+t().credits)}
function mineAsteroid(o){if(o.looted)return;o.looted=true;o.dead=true;runAsteroids++;score+=180;const drops=2+Math.floor(Math.random()*2);for(let i=0;i<drops;i++)dropPickup(o.x+(Math.random()-.5)*70,o.y+(Math.random()-.5)*70);if(Math.random()<.28)pickups.push({x:o.x,y:o.y,type:'tech',chip:null,value:1,r:14,life:20,phase:0,dead:false});showToast(t().asteroidMined);addParticles(o.x,o.y,'#d7a83c',38,260);shake=Math.max(shake,8)}
function damageWorldObject(o,damage){if(!o||o.dead||o.type!=='asteroid')return;o.hp-=damage;addParticles(o.x,o.y,'#c6aa76',4,90);if(o.hp<=0)mineAsteroid(o)}
function updateWorld(dt){cameraX+=(player.x-cameraX)*Math.min(1,dt*5.5);cameraY+=(player.y-cameraY)*Math.min(1,dt*5.5);explorationMax=Math.max(explorationMax,Math.hypot(player.x,player.y));ensureWorld();for(const o of worldObjects){if(o.dead)continue;o.phase+=dt*(o.spin||.2);o.hitCooldown=Math.max(0,(o.hitCooldown||0)-dt);const d=Math.hypot(player.x-o.x,player.y-o.y);if(d<Math.max(W,H)*.72+o.r)discoverWorldObject(o);if(o.type==='station'&&!o.looted){if(d<o.r+72){o.scan+=dt;if(o.scan>=3)salvageStation(o)}else o.scan=Math.max(0,o.scan-dt*.65)}if(o.type==='asteroid'&&d<o.r+player.r){const a=Math.atan2(player.y-o.y,player.x-o.x),push=o.r+player.r-d+2;player.x+=Math.cos(a)*push;player.y+=Math.sin(a)*push;if(o.hitCooldown<=0){o.hitCooldown=1;damagePlayer(9)}}}worldObjects=worldObjects.filter(o=>!o.dead||Math.hypot(o.x-player.x,o.y-player.y)<Math.max(W,H)*1.3)}
function explorationTarget(){let target=null,best=760*760;for(const o of worldObjects){if(o.dead||o.looted||o.type!=='asteroid')continue;const d=(o.x-player.x)**2+(o.y-player.y)**2;if(d<best){best=d;target=o}}return target}
function drawProceduralBackground(){ctx.fillStyle='#020308';ctx.fillRect(0,0,W,H);const grid=80,ox=((W/2-cameraX*.18)%grid+grid)%grid,oy=((H/2-cameraY*.18)%grid+grid)%grid;ctx.strokeStyle='#23608218';ctx.lineWidth=1;for(let x=ox-grid;x<W+grid;x+=grid){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,H);ctx.stroke()}for(let y=oy-grid;y<H+grid;y+=grid){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(W,y);ctx.stroke()}const cell=170,minX=Math.floor((cameraX-W)/cell),maxX=Math.ceil((cameraX+W)/cell),minY=Math.floor((cameraY-H)/cell),maxY=Math.ceil((cameraY+H)/cell);for(let gx=minX;gx<=maxX;gx++)for(let gy=minY;gy<=maxY;gy++){const rnd=seeded(gx,gy,91),wx=gx*cell+rnd()*cell,wy=gy*cell+rnd()*cell,sx=W/2+(wx-cameraX)*.32,sy=H/2+(wy-cameraY)*.32;if(sx<-5||sx>W+5||sy<-5||sy>H+5)continue;const r=.5+rnd()*1.7;ctx.globalAlpha=.18+rnd()*.65;ctx.fillStyle=rnd()>.92?'#ffd7a0':'#c8f3ff';ctx.fillRect(sx,sy,r,r)}ctx.globalAlpha=1}
function drawWorldObject(o){ctx.save();ctx.translate(o.x,o.y);if(o.type==='asteroid'){ctx.rotate(o.phase);const g=ctx.createRadialGradient(-o.r*.25,-o.r*.35,4,0,0,o.r);g.addColorStop(0,'#8a7b69');g.addColorStop(.45,'#39383b');g.addColorStop(1,'#121318');ctx.fillStyle=g;ctx.strokeStyle=o.discovered?'#d7a83c':'#4d4f55';ctx.lineWidth=o.discovered?2:1;ctx.beginPath();for(let i=0;i<12;i++){const a=i*Math.PI*2/12,rr=o.r*(.78+.22*seeded(Math.floor(o.x),Math.floor(o.y),i)());i?ctx.lineTo(Math.cos(a)*rr,Math.sin(a)*rr):ctx.moveTo(Math.cos(a)*rr,Math.sin(a)*rr)}ctx.closePath();ctx.fill();ctx.stroke();for(let i=0;i<4;i++){const a=i*1.63+o.phase*.35;ctx.fillStyle='#17181d';ctx.beginPath();ctx.arc(Math.cos(a)*o.r*.42,Math.sin(a)*o.r*.38,o.r*(.08+i*.012),0,Math.PI*2);ctx.fill()}if(!o.looted){const ratio=Math.max(0,o.hp/o.maxHp);ctx.fillStyle='#090b10';ctx.fillRect(-o.r*.65,-o.r-13,o.r*1.3,5);ctx.fillStyle='#d7a83c';ctx.fillRect(-o.r*.65,-o.r-13,o.r*1.3*ratio,5)}}else{ctx.rotate(o.phase*.08);ctx.strokeStyle='#55eaff';ctx.fillStyle='#0a1016';ctx.lineWidth=2;for(let i=0;i<3;i++){ctx.save();ctx.rotate(i*Math.PI*2/3);ctx.fillRect(-12,-70,24,48);ctx.strokeRect(-12,-70,24,48);ctx.restore()}ctx.beginPath();ctx.arc(0,0,46,0,Math.PI*2);ctx.fill();ctx.stroke();ctx.strokeStyle='#d7a83c';ctx.beginPath();ctx.arc(0,0,27,0,Math.PI*2);ctx.stroke();gfxGlow(0,0,7,o.looted?'#4c5666':'#55eaff',.75);if(!o.looted&&o.scan>0){ctx.strokeStyle='#ffd36a';ctx.lineWidth=5;ctx.beginPath();ctx.arc(0,0,92,-Math.PI/2,-Math.PI/2+Math.PI*2*Math.min(1,o.scan/3));ctx.stroke()}}ctx.restore()}
function drawRadar(){if(!player||state==='hangar')return;const x=W/2,y=H-92,r=W<600?38:48,range=1250;ctx.save();ctx.globalAlpha=.82;ctx.fillStyle='#05080dcc';ctx.strokeStyle='#d7a83c';ctx.lineWidth=1.4;ctx.beginPath();ctx.arc(x,y,r,0,Math.PI*2);ctx.fill();ctx.stroke();ctx.strokeStyle='#2de5f255';ctx.beginPath();ctx.arc(x,y,r*.52,0,Math.PI*2);ctx.stroke();for(const o of worldObjects){if(!o.discovered||o.dead)continue;const dx=(o.x-player.x)/range*r,dy=(o.y-player.y)/range*r;if(Math.hypot(dx,dy)>r-5)continue;ctx.fillStyle=o.type==='station'?'#55eaff':'#d7a83c';ctx.beginPath();ctx.arc(x+dx,y+dy,o.type==='station'?3.5:2.5,0,Math.PI*2);ctx.fill()}ctx.fillStyle='#ffffff';ctx.beginPath();ctx.moveTo(x,y-6);ctx.lineTo(x+4,y+5);ctx.lineTo(x-4,y+5);ctx.closePath();ctx.fill();ctx.restore()}
window.__SAG10__={snapshot:()=>({player:player?{x:player.x,y:player.y,ship:player.shipType,module:player.moduleId}:null,camera:{x:cameraX,y:cameraY},chunks:worldChunks.size,objects:worldObjects.length,discoveries,explorationMax,fields:fields.length})};`;
insertAfterLine("function fmtTime", worldCode, 'world generation');

replaceLine("function resetRun()", "function resetRun(){const sid=save.loadout.ship,s=ships[sid].stats,pb=shipPermanentBonus(),mb=moduleBonus(),diff=difficultyDefs[save.difficulty],zone=sectorDefs[save.selectedSector],rookie=Math.max(0,.26-(save.stats.runs||0)*.055),assist=(save.adaptiveAssist||0)*.045;player={x:0,y:0,r:20,hp:Math.round(s.hp*pb.hp),maxHp:Math.round(s.hp*pb.hp),shield:Math.round(s.shield*pb.shield),maxShield:Math.round(s.shield*pb.shield),speed:s.speed*pb.speed,angle:0,invuln:0,dash:0,dashMax:s.dash*mb.cooldown,dashTime:0,dashDx:0,dashDy:-1,emp:0,empMax:s.emp*mb.cooldown,damageMul:s.damage*pb.damage*mb.targetDamage,rateMul:s.rate,creditMul:s.credits*mb.credit,dropMul:mb.drop,damageTakenMul:mb.armor*diff.damage*Math.max(.52,1-rookie-assist),pickupRadius:7+mb.magnet,xpMul:mb.xp,coreChance:mb.coreChance,crit:mb.crit,multi:0,burn:0,railWidth:0,cluster:0,plasmaField:0,arcChain:0,flakGuard:0,overdrive:0,chipPulse:0,weaponBoost:{damage:0,rate:0,focus:0,total:0},fire:{primary:0,secondary:.4},enemyHpMul:diff.enemyHp*zone.enemyHp,enemySpeedMul:diff.enemySpeed*zone.enemySpeed,enemyBulletMul:diff.bullet,spawnInterval:diff.spawn*zone.spawn*(1+assist),rewardMul:diff.reward*zone.reward,repairMul:1+(save.skills.recovery||0)*.15,chipPower:1+(save.skills.chipTech||0)*.06,overchargeSkill:(save.skills.overcharge||0)>0,reviveCharges:(save.difficulty==='explorer'||(save.stats.runs||0)<5||(save.skills.lastStand||0)>0)?1:0,guaranteedRepairAt:(save.stats.runs||0)<5?24:0,shipType:sid,moduleId:mb.id,droneFire:.7,specterStrike:0,noDamage:0,markTimer:1.2,markedTarget:null};enemies=[];bullets=[];enemyBullets=[];rockets=[];plasmaBolts=[];beams=[];particles=[];pickups=[];explosions=[];fields=[];worldObjects=[];worldChunks.clear();boss=null;score=0;elapsed=0;wave=1;sector=zone.index;waveClock=0;spawnTimer=.55;level=1;xp=0;xpNeed=34;kills=0;bossesKilled=0;eliteKills=0;combo=1;maxCombo=1;comboClock=0;shake=0;pendingMajor=0;lastMajorAt=0;weaponChipsDropped=0;salvageCredits=0;salvageCores=0;discoveries=0;runStations=0;runAsteroids=0;explorationMax=0;directiveStartDistance=0;cameraX=0;cameraY=0;ensureWorld();setDirective();updateHUD()}", 'reset run');
replaceLine("function edgePos", "function edgePos(pad=55){const cx=player?.x||0,cy=player?.y||0,hw=W/2,hh=H/2,side=Math.floor(Math.random()*4);return side===0?{x:cx+(Math.random()-.5)*W,y:cy-hh-pad}:side===1?{x:cx+hw+pad,y:cy+(Math.random()-.5)*H}:side===2?{x:cx+(Math.random()-.5)*W,y:cy+hh+pad}:{x:cx-hw-pad,y:cy+(Math.random()-.5)*H}}", 'world edge spawn');
replaceLine("function nearestTarget()", "function nearestTarget(){let target=boss&&!boss.dead?boss:null,best=target?(boss.x-player.x)**2+(boss.y-player.y)**2:Infinity;for(const e of enemies){if(e.dead)continue;const d=(e.x-player.x)**2+(e.y-player.y)**2;if(d<best){best=d;target=e}}if(!target||best>760*760){const resource=explorationTarget();if(resource)target=resource}return target}", 'world targeting');
replaceLine("function targetList", "function targetList(n){const combat=[...enemies.filter(e=>!e.dead),...(boss&&!boss.dead?[boss]:[])].sort((a,b)=>((a.x-player.x)**2+(a.y-player.y)**2)-((b.x-player.x)**2+(b.y-player.y)**2));if(!combat.length){const resource=explorationTarget();if(resource)combat.push(resource)}return combat.slice(0,n)}", 'world target list');
replaceLine("function fireWeapon", "function fireWeapon(id,secondary=false){if(!id)return;const st=weaponStats(id,secondary);if(player.shipType==='specter'&&player.specterStrike>0){st.damage*=1.45;player.specterStrike=0;showToast(save.language==='de'?'SPECTER // PHASENSCHLAG':'SPECTER // PHASE STRIKE')}if(id==='laser')fireLaser(st,secondary);else if(id==='rail')fireRail(st,secondary);else if(id==='rocket')fireRocket(st,secondary);else if(id==='plasma')firePlasma(st,secondary);else if(id==='arc')fireArc(st,secondary);else fireFlak(st,secondary)}", 'ship weapon passive');
replaceLine("function firePlasma", "function firePlasma(st,secondary){const target=nearestTarget();if(!target)return;const a=Math.atan2(target.y-player.y,target.x-player.x);plasmaBolts.push({x:player.x,y:player.y,vx:Math.cos(a)*(secondary?215:185),vy:Math.sin(a)*(secondary?215:185),damage:st.damage,r:10+player.weaponBoost.focus*1.3,life:5,dead:false,secondary,field:player.plasmaField});addParticles(player.x,player.y,'#bd7cff',8,95)}", 'plasma evolution');
replaceLine("function fireArc", "function fireArc(st,secondary){const list=targetList(2+Math.min(2,player.multi)+Math.floor(player.weaponBoost.focus/2)+player.arcChain);if(!list.length)return;let x=player.x,y=player.y;list.forEach((target,index)=>{const mult=Math.pow(.76,index)*(target===boss?.72:1);hitTarget(target,st.damage*mult,'arc');beams.push({x1:x,y1:y,x2:target.x,y2:target.y,life:.18,max:.18,color:secondary?'#9fc2ff':'#75a7ff',width:4-index*.55});x=target.x;y=target.y});addParticles(player.x,player.y,'#75a7ff',7,105)}", 'arc evolution');
replaceLine("function hitTarget", "function hitTarget(target,damage,type){if(!target||target.dead)return;if(target.worldObject){damageWorldObject(target,damage);return}if(player&&player.crit&&Math.random()<player.crit){damage*=1.75;addParticles(target.x,target.y,'#ffffff',7,125)}if(player.moduleId==='targeting'&&target.marked>0)damage*=1.22;if(player.moduleId==='amplifier'&&(type==='plasma'||type==='arc'||type==='rocket'))damage*=1.1;target.hp-=damage;addParticles(target.x,target.y,type==='rail'?'#ffe08a':type==='laser'?'#55eaff':type==='arc'?'#75a7ff':type==='plasma'?'#bd7cff':'#ff8b66',3,85);if(target.hp<=0){if(target===boss)killBoss();else killEnemy(target)}}", 'target synergies');
replaceLine("function killEnemy", "function killEnemy(e){if(e.dead)return;e.dead=true;kills++;if(e.elite)eliteKills++;if(player.shipType==='interceptor')player.dash=Math.max(0,player.dash-.13);combo=Math.min(8,combo+.17);maxCombo=Math.max(maxCombo,combo);comboClock=3.2;score+=Math.round(e.score*combo);gainXP(e.xp);waveKills++;if(Math.random()<.12*player.dropMul)dropPickup(e.x,e.y);addParticles(e.x,e.y,e.color,15,210);checkDirective()}", 'interceptor passive');
replaceLine("function showMajorUpgrade", "function showMajorUpgrade(){state='upgrade';const pool=upgradePool().sort(()=>Math.random()-.5).slice(0,3),q=t();$('upgradeTitle').textContent=q.upgradeTitle;$('upgradeSubtitle').textContent=q.upgradeSub;$('choices').innerHTML='';for(const u of pool){const b=document.createElement('button');b.type='button';b.className='choice upgrade-card '+(u.weapon?'weapon-evolution':'system-upgrade');b.dataset.upgrade=u.id;b.innerHTML='<i>'+(u.weapon?q.evolution:q.upgradeTitle)+'</i><strong>'+q.upgradeNames[u.id]+'</strong><span>'+q.upgradeDescs[u.id]+'</span><em>'+q.install+'</em>';b.addEventListener('click',()=>{u.apply();if(player.shipType==='vanguard')player.hp=Math.min(player.maxHp,player.hp+Math.round(player.maxHp*.08));$('upgradeOverlay').classList.add('hidden');state='running';last=performance.now();updateHUD()});$('choices').appendChild(b)}$('upgradeOverlay').classList.remove('hidden')}", 'upgrade cards');
replaceLine("function upgradePool", "function upgradePool(){const p=[{id:'damage',apply:()=>player.damageMul*=1.18},{id:'rate',apply:()=>player.rateMul*=.85},{id:'multi',apply:()=>player.multi=Math.min(3,player.multi+1)},{id:'hull',apply:()=>{player.maxHp+=25;player.hp=Math.min(player.maxHp,player.hp+25)}},{id:'shield',apply:()=>{player.maxShield+=30;player.shield=player.maxShield}},{id:'speed',apply:()=>player.speed*=1.12},{id:'emp',apply:()=>player.empMax*=.78}];const equipped=[save.loadout.primary,save.loadout.secondary];if(equipped.includes('laser'))p.push({id:'laserBurn',weapon:true,apply:()=>player.burn++});if(equipped.includes('rail'))p.push({id:'railWidth',weapon:true,apply:()=>player.railWidth++});if(equipped.includes('rocket'))p.push({id:'rocketCluster',weapon:true,apply:()=>player.cluster++});if(equipped.includes('plasma'))p.push({id:'plasmaField',weapon:true,apply:()=>player.plasmaField=Math.min(2,player.plasmaField+1)});if(equipped.includes('arc'))p.push({id:'arcChain',weapon:true,apply:()=>player.arcChain=Math.min(2,player.arcChain+1)});if(equipped.includes('flak'))p.push({id:'flakGuard',weapon:true,apply:()=>player.flakGuard=Math.min(2,player.flakGuard+1)});return p}", 'weapon evolutions');
replaceLine("function setDirective", "function setDirective(){const types=wave===1?['kill','explore']:['kill','combo','salvage','explore'],type=types[Math.floor(Math.random()*types.length)],target=type==='kill'?10+wave*2:type==='combo'?Math.min(7,+(2+wave*.32).toFixed(1)):type==='explore'?5+wave*2:Math.min(5,1+Math.floor(wave/2));directive={type,target,done:false};waveKills=0;wavePickups=0;directiveStartDistance=explorationMax}", 'exploration directive');
replaceLine("function directiveProgress", "function directiveProgress(){return directive.type==='kill'?waveKills:directive.type==='combo'?+combo.toFixed(1):directive.type==='explore'?Math.floor((explorationMax-directiveStartDistance)/100):wavePickups}", 'exploration progress');
replaceLine("function collectPickup", "function collectPickup(p){if(p.type==='repair')player.hp=Math.min(player.maxHp,player.hp+30*player.repairMul);else if(p.type==='shield'){player.maxShield=Math.max(player.maxShield,35);player.shield=Math.min(player.maxShield,player.shield+35)}else if(p.type==='overdrive')player.overdrive=Math.max(player.overdrive,8);else if(p.type==='credits'){const value=p.value||25;salvageCredits+=value;score+=Math.round(value*.7);showToast(t().credits+' +'+value)}else if(p.type==='tech'){salvageCores+=p.value||1;score+=140;showToast(t().cores+' +'+(p.value||1))}else{const boost=player.weaponBoost;if(boost.total<8){let key=p.chip;if(boost[key]>=4.8)key=['damage','rate','focus'].find(x=>boost[x]<4.8)||key;boost[key]+=player.chipPower;boost.total++;player.chipPulse=.75;if(player.overchargeSkill)player.overdrive=Math.max(player.overdrive,4);const label=key==='damage'?t().chipDamage:key==='rate'?t().chipRate:t().chipFocus;showToast(t().weaponChip+' // '+label+' +'+boost[key].toFixed(1));explosions.push({x:p.x,y:p.y,r:0,max:78,life:.42,maxLife:.42,weapon:true})}else score+=90}if(player.moduleId==='magnet'){for(const e of enemies)if(!e.dead&&Math.hypot(e.x-p.x,e.y-p.y)<115)hitTarget(e,4+save.modules.magnet.level,'arc')}p.dead=true;wavePickups++;const color=p.type==='repair'?'#68ffc2':p.type==='shield'?'#9d82ff':p.type==='weapon'?'#ff5c46':p.type==='credits'?'#ffd36a':p.type==='tech'?'#55b8ff':'#55eaff';addParticles(p.x,p.y,color,p.type==='tech'?30:p.type==='weapon'?24:18,p.type==='tech'?260:p.type==='weapon'?230:185);checkDirective()}", 'module pickup synergy');
replaceLine("function damagePlayer", "function damagePlayer(amount){if(player.invuln>0||player.dashTime>0)return;player.noDamage=0;const shieldBefore=player.shield;let rem=amount*(player.damageTakenMul||1);if(player.shield>0){const a=Math.min(rem,player.shield);player.shield-=a;rem-=a}if(rem>0)player.hp=Math.max(0,player.hp-rem);player.invuln=.62;shake=Math.max(shake,9);addParticles(player.x,player.y,'#ff5878',16,190);if(player.shipType==='bastion'&&shieldBefore>0&&player.shield<=0){explosions.push({x:player.x,y:player.y,r:0,max:185,life:.45,maxLife:.45,emp:true});for(const e of enemies)if(!e.dead&&Math.hypot(e.x-player.x,e.y-player.y)<185)hitTarget(e,10,'arc');enemyBullets.forEach(b=>{if(Math.hypot(b.x-player.x,b.y-player.y)<185)b.dead=true});showToast(save.language==='de'?'BASTION // SCHILDIMPULS':'BASTION // SHIELD PULSE')}if(player.hp<=0&&player.reviveCharges>0){player.reviveCharges--;player.hp=Math.round(player.maxHp*.5);player.shield=Math.max(player.shield,Math.round(player.maxShield*.55));player.invuln=2.2;enemyBullets.length=0;showToast(save.language==='de'?'NOTSIGNAL // WIEDERHERGESTELLT':'EMERGENCY SIGNAL // RESTORED');addParticles(player.x,player.y,'#ffd36a',42,280);return}if(player.hp<=0)endRun()}", 'bastion passive');
replaceLine("function tryDash", "function tryDash(){if(state!=='running'||player.dash>0||player.dashTime>0)return;let dx=(keys.has('d')||keys.has('arrowright')?1:0)-(keys.has('a')||keys.has('arrowleft')?1:0),dy=(keys.has('s')||keys.has('arrowdown')?1:0)-(keys.has('w')||keys.has('arrowup')?1:0);if(Math.hypot(touch.x,touch.y)>.05){dx=touch.x;dy=touch.y}if(Math.hypot(dx,dy)<.05){dx=Math.cos(player.angle-Math.PI/2);dy=Math.sin(player.angle-Math.PI/2)}const m=Math.hypot(dx,dy)||1;player.dashDx=dx/m;player.dashDy=dy/m;player.dashTime=.17;player.dash=player.dashMax;player.invuln=.3;if(player.shipType==='interceptor')player.overdrive=Math.max(player.overdrive,1.8);if(player.shipType==='specter')player.specterStrike=1;addParticles(player.x,player.y,'#53e9ff',18,220)}", 'dash passives');
replaceLine("function tryEmp", "function tryEmp(){if(state!=='running'||player.emp>0)return;player.emp=player.empMax;if(player.moduleId==='capacitor')player.overdrive=Math.max(player.overdrive,3);let cleared=0;for(const b of enemyBullets)if(Math.hypot(b.x-player.x,b.y-player.y)<260){b.dead=true;cleared++}for(const e of enemies)if(!e.dead&&Math.hypot(e.x-player.x,e.y-player.y)<260){e.hp-=2;if(e.hp<=0)killEnemy(e)}explosions.push({x:player.x,y:player.y,r:0,max:260,life:.5,maxLife:.5,emp:true});showToast(t().empToast+(cleared?' · '+cleared:''));shake=7}", 'capacitor synergy');
replaceExact("save.stats.pilotXpEarned=(save.stats.pilotXpEarned||0)+pilotGain;save.sectorProgress", "save.stats.pilotXpEarned=(save.stats.pilotXpEarned||0)+pilotGain;save.stats.discoveries=(save.stats.discoveries||0)+discoveries;save.stats.stations=(save.stats.stations||0)+runStations;save.stats.asteroids=(save.stats.asteroids||0)+runAsteroids;save.explorationBest=Math.max(save.explorationBest||0,Math.round(explorationMax));save.sectorProgress", 'exploration rewards persistence');

replaceLine("function update(dt)", "function update(dt){elapsed+=dt;waveClock+=dt;spawnTimer-=dt;player.invuln=Math.max(0,player.invuln-dt);player.dash=Math.max(0,player.dash-dt);player.emp=Math.max(0,player.emp-dt);player.overdrive=Math.max(0,player.overdrive-dt);player.chipPulse=Math.max(0,player.chipPulse-dt);player.noDamage+=dt;player.markTimer-=dt;comboClock=Math.max(0,comboClock-dt);if(comboClock<=0)combo=Math.max(1,combo-dt*.8);toastTimer=Math.max(0,toastTimer-dt);if(toastTimer<=0)$('toast').classList.remove('show');if(waveClock>=48&&!boss)advanceWave();maybeMajorUpgrade();updateWorld(dt);if(player.guaranteedRepairAt&&elapsed>=player.guaranteedRepairAt){player.guaranteedRepairAt=0;pickups.push({x:player.x+70,y:player.y-45,type:'repair',chip:null,value:0,r:12,life:18,phase:0,dead:false});showToast(save.language==='de'?'KIWIMI // REPARATUR ABGEWORFEN':'KIWIMI // REPAIR DROP DEPLOYED')}if(player.moduleId==='armor'&&player.noDamage>6&&player.hp<player.maxHp)player.hp=Math.min(player.maxHp,player.hp+dt*(.7+.18*save.modules.armor.level));if(player.moduleId==='targeting'&&player.markTimer<=0){player.markTimer=2.4;const target=nearestTarget();if(target&&!target.worldObject){target.marked=3.2;player.markedTarget=target}}for(const e of enemies)e.marked=Math.max(0,(e.marked||0)-dt);if(boss)boss.marked=Math.max(0,(boss.marked||0)-dt);if(player.shipType==='carrier'){player.droneFire-=dt;if(player.droneFire<=0){player.droneFire=.92;const target=nearestTarget();if(target){const a=elapsed*2.1,dx=Math.cos(a)*30,dy=Math.sin(a)*30;beams.push({x1:player.x+dx,y1:player.y+dy,x2:target.x,y2:target.y,life:.12,max:.12,color:'#ffd36a',width:2});hitTarget(target,2.6*player.damageMul,'laser')}}}}", 'world update header');
replaceExact("player.x=Math.max(24,Math.min(W-24,player.x));player.y=Math.max(24,Math.min(H-24,player.y));", "", 'remove screen clamp');
replaceLine(" for(const b of bullets)", " for(const b of bullets){b.life-=dt;b.x+=b.vx*dt;b.y+=b.vy*dt;if(b.life<=0||Math.abs(b.x-player.x)>W*.9+260||Math.abs(b.y-player.y)>H*.9+260)b.dead=true;if(!b.dead){if(b.type==='flak'&&player.flakGuard>0){for(const hostile of enemyBullets)if(!hostile.dead&&Math.hypot(b.x-hostile.x,b.y-hostile.y)<b.r+hostile.r+4){hostile.dead=true;b.dead=true;addParticles(b.x,b.y,'#ffcf62',5,90);break}}if(!b.dead)for(const e of [...enemies,...(boss?[boss]:[]),...worldObjects.filter(o=>o.type==='asteroid'&&!o.dead&&!o.looted)]){if(!e||e.dead)continue;if(Math.hypot(b.x-e.x,b.y-e.y)<b.r+e.r){b.dead=true;const dist=Math.hypot(e.x-player.x,e.y-player.y),fall=b.type==='flak'?Math.max(.45,1-dist/520):1;hitTarget(e,b.damage*fall,b.type||'flak');break}}}}", 'projectile world bounds');
replaceLine(" for(const p of plasmaBolts)", " for(const p of plasmaBolts){p.life-=dt;p.x+=p.vx*dt;p.y+=p.vy*dt;particles.push({x:p.x,y:p.y,vx:(Math.random()-.5)*18,vy:(Math.random()-.5)*18,life:.18,max:.18,r:2.4,color:'#bd7cff'});if(p.life<=0)p.dead=true;if(!p.dead){for(const e of [...enemies,...(boss?[boss]:[]),...worldObjects.filter(o=>o.type==='asteroid'&&!o.dead&&!o.looted)]){if(!e||e.dead)continue;if(Math.hypot(p.x-e.x,p.y-e.y)<p.r+e.r){p.dead=true;explode(p.x,p.y,p.damage,82+player.weaponBoost.focus*8,0);if(p.field)fields.push({x:p.x,y:p.y,r:72+p.field*12,life:2.6,tick:0,power:p.damage*.16});if(e.worldObject)damageWorldObject(e,p.damage*.8);break}}}}", 'plasma fields');
replaceLine(" for(const b of enemyBullets)", " for(const b of enemyBullets){b.x+=b.vx*dt;b.y+=b.vy*dt;if(Math.abs(b.x-player.x)>W+320||Math.abs(b.y-player.y)>H+320)b.dead=true;if(!b.dead&&Math.hypot(b.x-player.x,b.y-player.y)<b.r+player.r){b.dead=true;damagePlayer(b.damage)}}", 'hostile world bounds');
replaceExact(" for(const p of particles){p.life-=dt;p.x+=p.vx*dt;p.y+=p.vy*dt;p.vx*=Math.pow(.05,dt);p.vy*=Math.pow(.05,dt)}", " for(const f of fields){f.life-=dt;f.tick-=dt;if(f.tick<=0){f.tick=.38;for(const e of enemies)if(!e.dead&&Math.hypot(e.x-f.x,e.y-f.y)<f.r+e.r)hitTarget(e,f.power,'plasma')}}for(const p of particles){p.life-=dt;p.x+=p.vx*dt;p.y+=p.vy*dt;p.vx*=Math.pow(.05,dt);p.vy*=Math.pow(.05,dt)}", 'field damage update');
replaceExact("enemies=enemies.filter(e=>!e.dead);", "enemies=enemies.filter(e=>!e.dead&&Math.hypot(e.x-player.x,e.y-player.y)<Math.max(W,H)*2.4);", 'enemy cleanup');
replaceExact("explosions=explosions.filter(e=>e.life>0);", "explosions=explosions.filter(e=>e.life>0);fields=fields.filter(f=>f.life>0);", 'field cleanup');

replaceLine("function updateHUD()", "function updateHUD(){if(!player)return;const q=t();$('gameTitle').textContent=q.title;$('controlsHint').textContent=q.controls;$('hpText').textContent=q.hull+' '+Math.ceil(player.hp)+' / '+player.maxHp;$('hpFill').style.width=Math.max(0,100*player.hp/player.maxHp)+'%';$('shieldRow').classList.toggle('hidden',player.maxShield<=0);$('shieldText').textContent=q.shield+' '+Math.ceil(player.shield)+' / '+player.maxShield;$('shieldFill').style.width=Math.max(0,100*player.shield/Math.max(1,player.maxShield))+'%';$('scoreText').textContent=q.score+' '+String(score).padStart(6,'0');$('timeText').textContent=q.time+' '+fmtTime(elapsed);$('bestText').textContent=q.best+' '+String(save.best).padStart(6,'0');$('waveText').textContent=q.sectorNames[save.selectedSector]+' · '+q.wave+' '+String(wave).padStart(2,'0');$('comboText').textContent=q.combo+' x'+combo.toFixed(1);$('levelText').textContent=q.level+' '+String(level).padStart(2,'0');$('xpText').textContent='XP '+Math.floor(xp)+' / '+xpNeed;$('xpFill').style.width=Math.min(100,100*xp/xpNeed)+'%';$('dashText').textContent=player.dash<=0?q.dashReady:'DASH '+player.dash.toFixed(1)+'s';$('empText').textContent=player.emp<=0?q.empReady:'EMP '+player.emp.toFixed(1)+'s';$('dashBtn').textContent=player.dash<=0?'DASH':player.dash.toFixed(1);$('empBtn').textContent=player.emp<=0?'EMP':player.emp.toFixed(1);$('dashBtn').classList.toggle('cooldown',player.dash>0);$('empBtn').classList.toggle('cooldown',player.emp>0);$('objectiveText').textContent=boss?q.objective:q.nextWave+' '+Math.max(0,Math.ceil(48-waveClock))+'s';if(directive){const label=directive.type==='kill'?q.directiveKill.replace('{n}',directive.target):directive.type==='combo'?q.directiveCombo.replace('{n}',directive.target):directive.type==='explore'?q.directiveExplore.replace('{n}',directive.target):q.directiveSalvage.replace('{n}',directive.target);$('directiveText').textContent=(directive.done?'✓ ':'')+label+' '+directiveProgress()+'/'+directive.target}$('weaponText').textContent=q.weaponNames[save.loadout.primary]+(save.loadout.secondary?' · '+q.weaponNames[save.loadout.secondary]:'')+(player.weaponBoost.total?' · +'+player.weaponBoost.total:'');const wait=lastMajorAt===0?Math.max(0,70-elapsed):Math.max(0,105-(elapsed-lastMajorAt));$('majorStatus').textContent=pendingMajor>0?(wait<=0?q.majorReady:q.majorIn+' '+Math.ceil(wait)+'s'):'';$('explorationText').textContent=q.distance+' '+Math.round(explorationMax)+' · '+q.discoveries+' '+discoveries;const near=worldObjects.filter(o=>o.discovered&&!o.dead&&!o.looted).sort((a,b)=>Math.hypot(a.x-player.x,a.y-player.y)-Math.hypot(b.x-player.x,b.y-player.y))[0];$('discoveryText').textContent=near?(near.type==='station'?q.station:q.resourceAsteroid)+' · '+Math.round(Math.hypot(near.x-player.x,near.y-player.y)):q.deepSpace;if(boss){$('bossFill').style.width=Math.max(0,100*boss.hp/boss.maxHp)+'%';$('bossText').textContent='FRONTIER WARDEN '+Math.max(0,Math.ceil(100*boss.hp/boss.maxHp))+'%'}}", 'exploration HUD');

const drawWorldCode = `function drawDeepSpaceObjects(){for(const o of worldObjects)if(!o.dead)drawWorldObject(o);for(const f of fields){ctx.save();ctx.globalAlpha=Math.max(0,f.life/2.6)*.38;ctx.fillStyle='#a969ff22';ctx.strokeStyle='#bd7cff';ctx.lineWidth=2;ctx.beginPath();ctx.arc(f.x,f.y,f.r*(1+.05*Math.sin(elapsed*5)),0,Math.PI*2);ctx.fill();ctx.stroke();ctx.restore()}}
function drawCarrierDrones(){if(!player||player.shipType!=='carrier')return;for(let i=0;i<2;i++){const a=elapsed*1.8+i*Math.PI,x=player.x+Math.cos(a)*34,y=player.y+Math.sin(a)*34;ctx.save();ctx.translate(x,y);ctx.rotate(a+Math.PI/2);ctx.fillStyle='#f5f2e9';ctx.strokeStyle='#d7a83c';ctx.lineWidth=1;ctx.beginPath();ctx.moveTo(0,-7);ctx.lineTo(6,5);ctx.lineTo(0,3);ctx.lineTo(-6,5);ctx.closePath();ctx.fill();ctx.stroke();gfxGlow(0,2,1.8,'#55eaff',.7);ctx.restore()}}`;
insertAfterLine("function drawBoss", drawWorldCode, 'deep space render helpers');
replaceLine("function draw()", "function draw(){ctx.save();ctx.clearRect(0,0,W,H);drawProceduralBackground();const sx=shake?(Math.random()-.5)*shake:0,sy=shake?(Math.random()-.5)*shake:0;ctx.save();ctx.translate(W/2-cameraX+sx,H/2-cameraY+sy);drawDeepSpaceObjects();for(const p of pickups)drawLoot(p);for(const b of bullets){ctx.fillStyle=b.color||'#ffb35c';ctx.beginPath();ctx.arc(b.x,b.y,b.r,0,Math.PI*2);ctx.fill()}for(const p of plasmaBolts){const glow=ctx.createRadialGradient(p.x,p.y,1,p.x,p.y,p.r*2.2);glow.addColorStop(0,'#ffffff');glow.addColorStop(.28,'#d9a5ff');glow.addColorStop(1,'#7e38b800');ctx.fillStyle=glow;ctx.beginPath();ctx.arc(p.x,p.y,p.r*2.2,0,Math.PI*2);ctx.fill()}for(const r of rockets){ctx.save();ctx.translate(r.x,r.y);ctx.rotate(Math.atan2(r.vy||0,r.vx||1)+Math.PI/2);ctx.fillStyle='#ff8b66';ctx.beginPath();ctx.moveTo(0,-9);ctx.lineTo(5,7);ctx.lineTo(-5,7);ctx.closePath();ctx.fill();ctx.restore()}for(const b of enemyBullets){ctx.fillStyle=b.color;ctx.beginPath();ctx.arc(b.x,b.y,b.r,0,Math.PI*2);ctx.fill()}for(const e of enemies)drawEnemy(e);drawBoss();if(player&&state!=='hangar'){drawShip();drawCarrierDrones()}for(const b of beams){ctx.globalAlpha=Math.max(0,b.life/b.max);ctx.strokeStyle=b.color;ctx.lineWidth=b.width;ctx.beginPath();ctx.moveTo(b.x1,b.y1);ctx.lineTo(b.x2,b.y2);ctx.stroke();ctx.globalAlpha=1}for(const e of explosions){ctx.globalAlpha=Math.max(0,e.life/e.maxLife);ctx.strokeStyle=e.emp?'#9d82ff':e.weapon?'#55eaff':'#ff8b66';ctx.lineWidth=e.emp?4:5;ctx.beginPath();ctx.arc(e.x,e.y,e.r,0,Math.PI*2);ctx.stroke();ctx.globalAlpha=1}for(const p of particles){ctx.globalAlpha=Math.max(0,p.life/p.max);ctx.fillStyle=p.color;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fill()}ctx.globalAlpha=1;ctx.restore();drawRadar();ctx.restore()}", 'camera renderer');

fs.writeFileSync(output, source);

let html = fs.readFileSync('sprint09.html', 'utf8');
html = html.replaceAll('Frontier Path 0.9', 'Deep Space 0.10').replaceAll('FRONTIER PATH 0.9', 'DEEP SPACE 0.10');
html = html.replace('S.A.G. Frontier Path — zugängliche Sektoren, Pilotenrang und verzweigter permanenter Skill Tree.', 'S.A.G. Frontier Deep Space — prozedurale Erkundung, Schiffspassiven, Waffen-Evolutionen und dauerhafte Progression.');
html = html.replace('<link rel="stylesheet" href="sprint09.css">', '<link rel="stylesheet" href="sprint09.css">\n  <link rel="stylesheet" href="sprint10.css">');
html = html.replace('<div id="majorStatus" class="major-status"></div>', '<div id="majorStatus" class="major-status"></div><div id="explorationText" class="exploration-status"></div><div id="discoveryText" class="discovery-status"></div>');
html = html.replace('<div class="section-heading loadout-heading"><span>MISSION CONTROL</span><h2>Loadout</h2></div>', '<section class="deep-space-brief"><div><span>PROZEDURALER TIEFENRAUM</span><h2>Die Karte endet nicht mehr am Bildschirmrand</h2><p>Fliege in jede Richtung, erschließe neue Raumsektoren und entdecke Ressourcen-Asteroiden sowie verlassene Stationen. Das Radar zeigt erkannte Ziele in deiner Umgebung.</p></div><div class="exploration-loop"><b>ERKUNDEN</b><i>→</i><b>ENTDECKEN</b><i>→</i><b>BERGEN</b><i>→</i><b>AUFRÜSTEN</b></div></section><div class="section-heading loadout-heading"><span>MISSION CONTROL</span><h2>Loadout</h2></div>');
html = html.replace('<script src="sprint09-source.js"></script>', '<script src="sprint10-source.js"></script>');
fs.writeFileSync('sprint10.html', html);
console.log('Built Sprint 0.10 Deep Space', source.length, html.length);
